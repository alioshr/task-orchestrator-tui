/**
 * Data Adapters
 *
 * Barrel export for adapter implementations
 */

export { DirectAdapter } from './direct';
export type {
  DataAdapter,
  Result,
  SearchParams,
  FeatureSearchParams,
  TaskSearchParams,
} from './types';
